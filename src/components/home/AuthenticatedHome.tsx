/**
 * @deprecated Use `Home` from '@/components/home/Home' directly.
 * Kept as a thin re-export for backward compatibility.
 * Auth-aware view state is derived internally from the user + scan status.
 */
export { default } from './Home';
